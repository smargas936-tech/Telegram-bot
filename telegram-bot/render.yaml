services:
  - type: worker
    name: telegram-bot
    env: python
    region: frankfurt
    plan: starter  # бесплатный план
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
